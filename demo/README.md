# Demo


## Setup

```
$ docker-compose up -d
```

## Grafana

```
$ open localhost:3000
```

Dashboard: demo-flamgraph

## Producing stack traces

```
$ curl localhost:8000
$ curl localhost:8000
$ curl localhost:8000
$ curl localhost:8000
$ curl localhost:8000
...
```
