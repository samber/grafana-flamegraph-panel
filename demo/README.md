# Demo


## Setup

```
$ docker-compose up -d
```

## Grafana

```
$ open localhost:3000 
$ login with admin / admin credentials
```

Dashboard: demo-flamgraph

## Producing stack traces

```
$ curl localhost:8080
$ curl localhost:8080
$ curl localhost:8080
$ curl localhost:8080
$ curl localhost:8080
...
```
